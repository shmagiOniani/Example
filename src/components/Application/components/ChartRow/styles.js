export const styles = (theme) => ({
  chartpaper: {
    justifyContent: "space-between",
    padding: "24px",
    borderRadius: "20px",
    height: "350px",
    "& > *": {
      textAlign: "start",
    },
  },
});
